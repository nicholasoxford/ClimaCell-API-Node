let loadJson,saveJSON,chalk;_fde‍.x([["get_clima",()=>get_clima]]);_fde‍.w("./jsonCRUD",[["loadJson",["loadJson"],function(v){loadJson=v}],["saveJSON",["saveJSON"],function(v){saveJSON=v}]]);_fde‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);


const ProgressBar = require("./ProgressBar");

var axios = require("axios");
let listofTemps = [];
let mongoObj = [];
       async function get_clima(api) {
  const apikey = api;
  const climaJSON = loadJson("clima-node/src/json/longLangByCity.json");
  const Bar = new ProgressBar();

  let c;
  let apiLimit = 50;
  Bar.init(apiLimit);

  // console logs
  _fde‍.g.console.log("", chalk.green.bold("Downloading weather data."));

  for (c = 0; c < apiLimit; c++) {
    let n = c + 1;
    Bar.update(n);
    await axios
      .get("https://api.climacell.co/v3/weather/realtime?lat=" + climaJSON[c][1].toString() + "&lon=" + climaJSON[c][0].toString() + "&unit_system=us&fields=temp%2Cfeels_like&apikey=" + apikey)
      .then((response) => {
        let z = JSON.stringify(response.data.temp.value);
        let x = { temp: z, city: climaJSON[c][3], state: climaJSON[c][4], longitude: climaJSON[c][0], latitude: climaJSON[c][1], date: new Date() };
        let n = [z, climaJSON[c][3], climaJSON[c][0], climaJSON[c][1]];
        mongoObj.push(x);
        listofTemps.push(n);
      })
      .catch((error) => {
        _fde‍.g.console.log(error);
      });
  }
  console.log("");
  saveJSON("clima-node/src/json/finaltemps.json", listofTemps);
  return mongoObj;
}
