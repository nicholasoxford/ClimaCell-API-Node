let loadJson,saveJSON,chalk;_eba‍.x([["get_clima",()=>get_clima]]);_eba‍.w("./jsonCRUD",[["loadJson",["loadJson"],function(v){loadJson=v}],["saveJSON",["saveJSON"],function(v){saveJSON=v}]]);_eba‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);

const ProgressBar = require("./ProgressBar");

var axios = require("axios");
let listofTemps = [];
       async function get_clima(api) {
  const apikey = api;
  const climaJSON = loadJson("clima-node/src/json/longLangByCity.json");
  const Bar = new ProgressBar();

  let c;
  let apiLimit = 50;
  Bar.init(apiLimit);
  _eba‍.g.console.log(chalk.green.bold("Downloading wether data."));
  for (c = 0; c < apiLimit; c++) {
    Bar.update(c);
    await axios
      .get("https://api.climacell.co/v3/weather/realtime?lat=" + climaJSON[c][1].toString() + "&lon=" + climaJSON[c][0].toString() + "&unit_system=us&fields=temp%2Cfeels_like&apikey=" + apikey)
      .then((response) => {
        let z = JSON.stringify(response.data.temp.value);
        let x = [z, climaJSON[c][3], climaJSON[c][0], climaJSON[c][1]];
        listofTemps.push(x);
      })
      .catch((error) => {
        _eba‍.g.console.log(error);
      });
  }
  saveJSON("clima-node/src/json/finaltemps.json", listofTemps);
}
