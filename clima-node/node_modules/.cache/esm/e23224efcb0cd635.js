let chalk;_cb0‍.x([["mongoWrite",()=>mongoWrite]]);_cb0‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);var mongoose = require("mongoose");


       function mongoWrite(listofTemps) {
  // make a connection
  let uri = "mongodb+srv://weatheradmin:Pelo30337@cluster0-c4n9i.mongodb.net/weatherDB?retryWrites=true";
  mongoose.connect(uri, { useUnifiedTopology: true, useNewUrlParser: true });
  // get reference to database
  var db = mongoose.connection;

  db.on("error", _cb0‍.g.console.error.bind(_cb0‍.g.console, "connection error:"));

  db.once("open", function () {
    console.log("Succesfully connected to your MongoDB instance.");

    // define Schema
    var tempSchema = mongoose.Schema({
      temp: String,
      city: String,
      state: String,
      longitude: Number,
      latitude: Number,
      date: Date,
    });
    let collection = "temps";
    // compile schema to model
    var Temp = mongoose.model("Temp", tempSchema, collection);

    // save multiple documents to the collection referenced by Book Model
    Temp.collection.insertMany(listofTemps, function (err, docs) {
      if (err) {
        return _cb0‍.g.console.error(err);
      } else {
        _cb0‍.g.console.log(listofTemps.length, "documents inserted to Collection:", chalk.green.bold(collection));
        db.close();
      }
    });
  });
}
